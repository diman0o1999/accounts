
<%= simple_form_for(@report) do |f| %>
  <%= f.input :category_id, as: :radio_buttons, collection: Category.all, wrapper: :vertical_collection_inline %>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>